# 06 · 内置工具与插件

## 工具是什么

顾清影 的工具不是给用户敲的 CLI 命令，而是**给模型看的功能说明书**。你说"帮我查天气"，模型判断需要调用 `get_weather`，顾清影 执行工具并把结果喂回模型，模型再用大白话回答你。

## 真相源与注册机制

- 每个工具的元数据是：`name`、`display_name`（中文显示名）、`description`、参数 JSON Schema、`always_loaded`、`load_policy`（summary/group/hidden）、`groups`、`timeout_seconds`；权限（ReadOnly / Presentation / Writes）由 Rust 代码里的 `.writes()` / `.presentation()` 决定。
- **描述以 `src/tools/descriptions/*.json` 为唯一真相**：build.rs 扫描该目录自动生成 include 清单编译进二进制（丢进目录即生效），注册时（`ToolSpec::apply_built_in_description`）整体覆盖 Rust 代码里的占位描述与参数。唯一例外是 `load_skill`——它的描述是运行时拼的技能目录，但加载策略、分组等元数据仍来自 JSON。
- 四个工具**没有** JSON 文件、用 Rust 内联描述：`job`（后台任务查询/停止）、`query_token_usage`（token 用量）、`load_tools`（懒加载目录本身）、`send_subagent_message`。清单在 `shape_tests::RUST_ONLY_DESCRIPTIONS`，其余工具缺 JSON 会报红。
- 工具分组的摘要与中文名在 `src/tools/descriptions/groups.json`。

## 一条流水线装三个面

组装入口在 `src/tools/compose.rs::compose_registry(config, paths, persona 清单, 场所)`（09-10 分层架构阶段 4，取代以前各写一遍的 `builtin_registry` / `dev_registry` / `restricted_platform_registry` 三张表，三个名字保留为薄包装）。注册单元是一张有序表 `UNITS`，插件 id、显示名与引导开关的唯一真相源是 `src/config/plugin_catalog.rs`：

1. **core**：不看人格的底座——run_command + job、apply_patch、todowrite、goal、web 抓取/搜索、vision、MCP、subagent 子代理、load_tools。
2. **扩展**：按 persona 清单启用。子系统（记忆、技能、语音、人格提醒、情绪）挂进回合流水线多个点；插件只往工具面加东西（id 见 `config::PLUGIN_IDS`：files、album、usage_query、alarm、exchange_rate、map、express、archlinux、api_quota、print_image、memes、platform_outreach、web_images、image_generation、knowledge_base、ledger、scripts）。每一项同时受 config 里机器级开关约束：persona 只能在「本机装了的」里挑。
3. **场所**：`Surface { trust, interactive_questions }`。External（QQ 群、远端 WebUI 成员）只留 `trust == external` 的工具；能弹面板的入口才给 `ask_question`；WebUI 会话另加 `present_artifact` 与 `share_file`。

| 面 | 怎么来 | 内容 |
|---|---|---|
| Normal | 当前人格清单（缺省全开）+ Owner 场所 | 完整工具面 |
| Dev | 保留人格 `dev` 的清单（`core_only`：子系统全关、插件只留 platform_outreach）+ Owner 场所 | 极简：core 那十件 |
| 受限平台 | 当前人格清单 + External 场所 | 只剩 `trust: external` 的工具：web_fetch / web_search / use_meme / manage_meme / load_skill / load_tools / generate_image（开着时）/ get_exchange_rate（开着时）+ 头部写了 `Trust: external` 的脚本 |

权限位不再是「注册位置」：内置工具在 `descriptions/*.json` 里写 `"trust": "external"`，脚本在头部写 `Trust: external`，场所层按它筛。三个面的 tools 数组与合并前逐字节相同（`src/tools/fixtures/registry-shapes.json` 钉着；Normal 面多了 `load_skill` / `manage_skill` 两件，那是以前 `build_tool_registry` 在表外补的）。

**persona.toml**（`data/personas/<人格>/persona.toml`，缺省不用写）：

```toml
[subsystems]
memory = true            # 关掉 = 不建库、不联想、不写日记、无记忆工具
skills = true
persona_reminder = true
voice = true
emotion = true           # 只在通讯平台生效

[plugins]
enabled = ["ledger", "memes", "scripts", "mcp"]   # 不写 = 本机装了的全部
scripts = ["gpustoggle"]     # 脚本按 id 逐个勾;不写 = 全部(只在 scripts 插件开着时有意义)
skills = ["pdf"]             # 技能按名字逐个勾;不写 = 全部。skill-creator / script-creator 不受它管
mcp = ["filesystem"]         # MCP 服务器按 id 逐个勾;不写 = 配置里开着的全部(只在 mcp 插件开着时有意义)
```

三个白名单全开时不写（= 以后装进来的也自动可见），只有关过东西才写成明细。**自定义人格**下内置脚本（`<system>/personas/default/`）与非平台级内置技能是可选件：没写清单 = 一件不挂（纯净状态），在清单里点了名才挂——引导的「功能」屏勾一下就写进去。人格**自己那一层**（`extensions/scripts/personas/<人格>/`、同名技能目录）不受白名单管：它自己 `manage_script register` 出来的脚本、专门给它放的技能永远可用；`manage_script list` 看到的目录与真挂上的工具面是同一份。终端引导与 WebUI 成员引导共用一份「自选功能」表（`src/config/feature_catalog.rs`）：记忆、知识库、技能、MCP 以及文件/看图/搜图/用量/脚本插件本身常开不摆开关；给开关的内置插件只有闹钟、汇率、Arch、API 额度、表情包、生图、记账；每个脚本、每个非平台级技能逐个勾。

`dev` 人格没有文件时按 `core_only` 处理（核心工具 + `platform_outreach`，**不带 MCP**，09-13 用户拍板）；写一个 `personas/dev/persona.toml` 就能给开发模式加东西。

## 懒加载：stub / hybrid / full

`tools.loading_mode` 三档，默认 **stub**：

- **stub**：懒工具全部以"真名 + 一句话摘要 + 宽松参数壳"常驻工具目录，工具数组整会话字节恒定——这是为**前缀缓存命中**做的设计（见 [10-系统设计与架构](10-系统设计与架构.md)）。完整参数合同用 `load_tools` 按需取，结果走对话尾部。
- **hybrid**（别名 lazy）：加载后把完整定义补进工具数组。
- **full**：全部完整定义直接常驻。

`load_policy` 决定单个工具的懒加载行为：`summary` 允许未加载直接调用；`group` 必须先按组加载；`hidden` 不经 `load_tools` 暴露。

## 内置工具清单

注册表列：N=Normal，D=Dev，P=受限平台，W=WebUI 追加。加载列按默认 stub 模式：常驻=完整定义常驻；懒=stub 常驻、合同按需取（括号内为组）。标 **S** 的是内置脚本（`src/scripts/personas/default/`，装到 `/usr/share/gqy/scripts/personas/default/`）：Rust 里没有实现，描述、参数、分组、信任位全在脚本头部（09-10 分层架构阶段 3）；`P` 来自头部的 `Trust: external`。

### 系统与文件

| 工具 | 用途 | 注册表 | 加载 |
|---|---|---|---|
| `run_command` | 执行 shell 命令/脚本，`background=true` 转后台任务 | N·D | 常驻 |
| `job` | 后台任务查询/停止（action=status/stop） | N·D | 懒 |
| `read` | 读文件/列目录（分页），认 `artifact:`/`kb:` 前缀 | N | 常驻 |
| `edit` | 补丁式增/改/删文件系统文件 | N·D | 常驻 |
| `kb` | 补丁式写知识库（增/改/删，元数据与语义索引同步） | N | 懒 |
| `artifact` | 补丁式写 WebUI Artifact 库（WebUI 会话注册） | WebUI | 常驻 |
| `glob` | 通配符找文件 | N | 常驻 |
| `grep` | ripgrep 搜索文本 | N | 常驻 |
| `trash_path` | 移入回收站（拒绝根/家目录等危险目标） | N | 懒（dev） |
| `check_os_info` | 只读系统信息 | N | 懒（diagnostics） |
| `github` | commit / 提 PR / 提 issue / 评论 / gh 直通；用户固定 author，顾清影 自动挂 `Co-Authored-By`；缺省用户自己的 gh 身份，`as_bot=true` 才用 `gqy github login` 登录的 bot（凭据隔离在 `~/.gqy/github/`） | N·D | 懒（dev） |
| `read_clipboard` | 读剪贴板文本/图片（S） | N | 懒 |
| `query_token_usage` | 查 LLM token 用量（QQ 侧有同名包装版） | N | 常驻 |

### 任务与规划

| 工具 | 用途 | 注册表 | 加载 |
|---|---|---|---|
| `subagent` | 子代理任务（`dev` 开发模式、`tier` 四档 lite/cheap/standard/flagship、background、resume_id；描述是常量字节，不再列池成员） | N·D | 常驻 |
| `todowrite` | 会话任务清单 | N·D | 懒 |
| `goal` | 长任务目标（会话持久）；action=get/create/edit/pause/resume/complete/blocked | N·D | 懒 |
| `github` | GitHub 操作（action=status/commit/pr_create/issue_create/comment/gh）；commit、PR、issue 由代码统一追加 `Co-Authored-By: 顾清影 <邮箱>`（issue / PR 正文里名字是指向项目主页的 Markdown 链接，commit 里保持纯文本，否则 GitHub 认不出 co-author）；默认用你自己的 gh/git，明确要求时 `as_bot=true` 切到 `gqy github login` 登录的独立账号（凭据隔离在 `~/.gqy/github/`）；`tools.github.enabled` 控制 | N·D | 懒 |
| `alarm` | 闹钟（action=set/list/cancel，独立 worker 进程） | N | 懒（alarms） |
| `ledger` | 记账（action=add/list/summary/update/delete；外币自动按当日汇率换算并冻结快照；超预算时结果带 budget 字段） | N | 懒 |
| `manage_ledger` | 账本/账户/分类/预算管理 | N | 懒（ledger） |
| `ask_question` | 向用户弹结构化提问卡（可交互界面才注册） | N·D | 常驻 |

### 网络与资料

| 工具 | 用途 | 注册表 | 加载 |
|---|---|---|---|
| `web_search` | 多供应商网络搜索（详见 09） | N·D·P | 常驻 |
| `web_fetch` | 抓网页转 Markdown/文本/HTML | N·D·P | 常驻 |
| `get_weather` | 天气查询（S） | N·P | 懒（web） |
| `query_moegirl` | 萌娘百科（S） | N·P | 懒（acg） |
| `online_man` | 在线 man 手册（action=search/read）（S） | N | 懒（linux-docs） |
| `fcitx5_input_method_wiki_qurey` | Fcitx5 官方 wiki（S） | N | 懒（diagnostics） |
| `game_compat` | 游戏 Linux 兼容性：ProtonDB + AreWeAntiCheatYet + caniplayonlinux（S） | N·P | 懒（gaming） |
| `archwiki_query` | Arch Wiki 查询 | N | 懒 |
| `archlinux_news` | Arch 新闻 | N | 懒（arch） |
| `archlinux_official_package_query` | 官方包查询 | N | 懒（arch） |
| `aur` | AUR RPC（action=search/info/status） | N | 懒（arch） |
| `review_aur_package` / `install_aur_package` | PKGBUILD 审查与确认后安装（archlinux 插件） | N | 懒（arch） |

### 知识、记忆、技能、脚本

| 工具 | 用途 | 注册表 | 加载 |
|---|---|---|---|
| `search_knowledge_base` | 搜索知识库（by=content/name）；读走 `read` 的 `kb:` 前缀，写走 `kb` 工具 | N | 懒 |
| `remember_fact` | 长期记忆写入；`kind=correction` 记下用户纠正过的事与原因 | N·D | 常驻 |
| `recall_memories` | 记忆检索/召回 | N·D | 懒 |
| `search_evicted_context` | 搜索被挤出上下文窗口的旧对话 | N·D | 懒 |
| `load_skill` | 加载技能（描述=动态技能目录） | N·D·P | 常驻 |
| `manage_skill` | 技能创作（action=create/update/delete/publish/list_drafts） | N | 懒（skills） |
| `manage_script` | 脚本工具注册/注销/清单（action=register/unregister/list；register 可给任意路径，自动复制进脚本目录） | N | 懒（scripting） |

Dev 模式的记忆库切到独立命名空间（保留人格 "dev"），与默认人格互不可见。

### 多媒体与展示

| 工具 | 用途 | 注册表 | 加载 |
|---|---|---|---|
| `generate_image` | 文生图/图生图（受限平台每请求限一张） | N·P | 懒 |
| `search_web_images` | 多源并行搜网图 | N | 懒 |
| `vision_analyze` | 看图/看视频（本地路径或 URL，可路由到专用视觉模型；平台回合里还认 `context_image_N` 与聊天里的 `file_<消息id>_<n>` 视频/图片文件 id，按需懒下载） | N·D | 常驻 |
| `print_image` | 终端渲染/显示图片 | N | 懒（images） |
| `album` | 图库：存/找/发普通图片（action=save/search/list/show/delete），按人格分库，与表情包分开 | N·P | 懒（images） |
| `use_meme` | 表情包搜索与发送（action=search/show） | N·P | 常驻 |
| `manage_meme` | 表情包库管理（action=add/update/delete） | N·P | 懒（memes） |
| `present_artifact` | 把已完成的产物文件放进 WebUI Artifact 演示区 | W | 常驻 |
| `share_file` | WebUI 文件分享（引用模式零复制 / snapshot 托管） | W | 常驻 |

### 娱乐与杂项

| 工具 | 用途 | 注册表 | 加载 |
|---|---|---|---|
| `divine` | 占卜（method=zhouyi/tarot/fortune/dice）（S） | N·P | 懒（divination） |
| `codec` | 哈希与 base64/hex/url/html/rot13 编解码（S；blake3 需可选的 python-blake3） | N·P | 懒（utility） |
| `scientific_calculator` | 科学计算器（S） | N·P | 懒（utility） |
| `get_exchange_rate` | 汇率查询 | N·P | 懒 |
| `map_search` | 地图：地址/POI 查坐标、坐标查地址；WebUI 渲染成带标记点的地图卡片 | N·P | 懒（web） |
| `express_query` | 快递查询：单号查物流轨迹；WebUI 渲染成时间线卡片 | N·P | 懒（web） |
| `query_api_quota` | DeepSeek/OpenRouter API 额度 | N | 懒 |
| `query_deepseek_status` | DeepSeek 服务状态（S） | N·P | 懒（systeminfo） |

### 动态工具

- **MCP**：`mcp.servers` 配置的 stdio server 工具自动注册（N·D）。
  - 工具列表（tools/list）按 server 配置缓存在 daemon 进程里：成功的列表在配置不变时不再重新拉；起不来或超时的 server 记 error 日志、跳过其工具，60 秒后下次重建注册表时再试。多个 server 并行列举，列举预算单独封顶（最多 15 秒 + 5 秒余量），不受 `timeout_seconds`（工具调用超时）影响。
  - daemon 启动最多等 3 秒上下文现算（含 MCP 列举），超过就先就绪、算好再回填，不可达的 MCP server 不会再让 daemon 起不来。
- **脚本**：`~/.gqy/data/scripts/`（及 `personas/<人格>/`）里带 shebang 和 `# Description:` 头部的可执行文件自动成为工具；头部还能声明 `Trust: external`（给 QQ 群等不可信入口）、`Permission`、`Example`、`Hint`、`Requires`，stdout 里 `GQY-IMAGE: 路径 | 说明` 一行回传图片；`manage_script` 可把任意路径的脚本复制进来注册。缺描述的文件会出现在 `load_tools` 描述的 `<unregistered_scripts>` 里提示注册。接口契约见 `docs/scripts/README.md`。

## 三个存储域与 read 前缀

写入按域分工具（08-21 二次裁定：工具名是最强的能力广告，域内聚合、域间分名）：`edit`=文件系统、`artifact`=Artifact 库、`kb`=知识库，三者都收 `*** Begin Patch / *** End Patch` 补丁。`read` 统一读三域，用前缀选域：

编辑与读取统一成两个工具，用路径前缀选目标存储，**不再各开一套工具**：

| 前缀 | 目标 |
|---|---|
| 裸路径 | 文件系统 |
| `artifact:名字` | 当前会话的 WebUI Artifact 库（`read` 传 `artifact:` 空名 = 列清单） |
| `kb:相对路径` | 知识库（写入走知识库导入/删除，元数据同步） |

一个补丁可含多个 Add/Update/Delete File 段；`edit` 收到带 `artifact:`/`kb:` 前缀的补丁会报错指路到对应工具。旧工具 `apply_patch`（更名为 `edit`）、`read_file`（更名为 `read`）、`write_file`、`edit_file`、`edit_string`、`create_artifact`、`apply_artifact_patch`、`read_artifact`、`upload_text_to_knowledge_base`、`edit_knowledge_base_file`、`read_knowledge_base_file`、`remove_knowledge_base_file`、`claude_code` 均已移除或并入。

## 平台（QQ）专用工具

平台会话在受限注册表之上内联注册（`src/platforms/tool.rs`、`src/platforms/file_reader.rs` 与 `src/platforms/plugins/` 各插件的 `register_tools`）：

### 宿主侧

| 工具 | 用途 | 来源 |
|---|---|---|
| `send_message_to_user` | 发文本/图片/文件到当前会话（图片/文件仅宿主工具权限会话） | tool.rs |
| `qq_mention_users` | 群聊原生 @ 成员（仅群聊注册） | tool.rs |
| `query_token_usage` | token 用量（平台包装版，口径同 WebUI） | tool.rs |
| `read_platform_file` | 读会话里上传的文本文件（拒绝二进制，128 KiB 上限；视频/图片文件会提示改用 `vision_analyze` 传同一个 id） | file_reader.rs |

QQ 里的文件与视频统一走懒下载：入站时只记 id（文件段 / 群文件上传通知 / 视频段），当轮正文与历史里渲染成 `[文件 id=file_<消息id>_<n>, label=名字]` 或 `[视频 id=…]`，模型调用工具时才下载到 `<cache>/platform_files/qq/`（普通文件 50MB、视频 200MB 上限，目录总量 1GB）。下载来源依次尝试：段自带 url → `get_group_file_url` / `get_private_file_url` → NapCat 通用 `get_file`（认 http 直链、桥同机的本地路径、base64 三种返回）。看视频需要活跃模型池整池支持视频输入，或配置 `plugins.vision.video_provider_id` / `video_model`。

### 插件侧

| 工具 | 用途 | 插件 |
|---|---|---|
| `qq_withdraw_message` | 撤回自己发的消息 | message_recall |
| `search_real_chat_history` | 搜索真实聊天记录 | message_history |
| `get_real_chat_activity_ranking` | 群活跃度排行 | message_history |
| `delete_real_chat_history` | 删除聊天记录 | message_history |
| `get_group_members_info` | 群成员信息 | message_history |
| `get_avatar` | 取头像 | message_history |
| `qq_group_manage` | 群管理（禁言等） | group_management |
| `qq_group_manage_history_query` | 群管理操作历史 | group_management |
| `manage_platform_access` | 平台访问权限管理 | access_manager |
| `save_current_message_meme` | 收藏当前消息里的表情包 | meme_collector |
| `delete_referenced_meme` | 删除被引用的表情包 | meme_collector |
| `query_qq_relationship` | 查好感度/关系 | real_context |
| `add_active_judgement_skip_qq` / `remove_active_judgement_skip_qq` | 主动发言判定跳过名单增删 | real_context |

宿主工具与管理类插件工具按会话权限注册：群聊宿主工具默认只对管理员开放，私聊需白名单放行（见 [13-QQ与通讯平台](13-QQ与通讯平台.md)）。

## 权限与护栏

- 权限三档 ReadOnly / Presentation / Writes，由代码决定（JSON 里的 `permission` 字段只是给人看的）。受限平台除 `generate_image` 外全部只读。
- 守卫（guard）只拒不放：同一轮里 `review_aur_package` 之后直接 `install_aur_package` 会被拦（必须先经用户确认）；`tools.command_deny` 提供 `run_command` 的子串黑名单。
- 每工具超时可在 JSON 里用 `timeout_seconds` 指定（0=豁免，缺省吃 `tools.default_timeout_secs` 兜底）。
- 删除文件默认走回收站（`trash_path`）；`edit` 写入原子化并渲染 unified diff，Artifact 目录额外防 symlink 逃逸。
