# 14 · 参与开发

> ⚠️ **临时版本提示**：本文档由 DeepSeek 根据当前 顾清影 源码逐行整理，属于临时参考版本；正式版文档仍在施工中，内容可能与最新代码存在出入。请以 `gqy -h`、`gqy config`、仓库 README 及源码为准。

## 1. 环境要求

- Rust 1.89+（`rust-version = "1.89"`）
- Linux 或 macOS（Apple 芯片与 Intel 都能编译；Windows 不支持）
- 可选依赖：`chafa`、`ripgrep`、`git`、`fish/bash/zsh`
- 字体：系统包提供 Noto CJK/Emoji；源码开发时字体目录会回退到 `assets/fonts`

## 2. 构建与运行

```bash
git clone https://github.com/yxxbc/gqy-agent.git
cd gqy-agent
cargo build                    # debug
cargo run -- --help            # 二进制名是 gqy
cargo build --release          # 只生成 gqy
cargo build --release --features voice   # 额外生成 gqy-voice（链接 sherpa-onnx）
```

release profile：`lto = "thin"`、`strip = "symbols"`、`codegen-units = 1`（完整 release 编译约 5 分钟）。

- **语音**：预编译包和 Nix 包都不带 `gqy-voice`，要本地语音就得带 `--features voice` 从源码编译。把 `target/release/gqy` 和 `gqy-voice` 放进同一个 `PATH` 目录（如 `~/.local/bin`），daemon 启动时会在 `gqy` 旁边找 `gqy-voice`。开发机日常本地构建不带语音：`cargo install --path . --locked`，只在要改或验收语音时才加 `--features voice`。**不要**在同一台机器上再 `nix profile install`：`~/.cargo/bin` 会把 Nix 版遮住，两边版本对不上很难排查。
- **改默认人格**：内置的顾清影人格在 `src/prompts/gqy.md`（另有 `gqy.hint.md`、`gqy-dialogs.md`），改完要重新编译。只想换个人格的话不用改源码，用 `gqy config` →「自定义提示词」或 WebUI 的人格页新建一个即可。
- **Nix**：`nix develop` 进入带 Rust 工具链和 ONNX Runtime 的开发环境；`nix build .#gqy-src` 在 `./result` 下得到和安装版一样的完整布局。改动资源、外部命令依赖、发布平台时 Nix 打包要同步修改，见 [18-Nix安装开发与发布](18-Nix安装开发与发布.md) §3。
- **发布新版本**：见 [18-Nix安装开发与发布](18-Nix安装开发与发布.md) §4。改动的用户可见说明写进仓库根目录 `CHANGELOG.md` 的 `[Unreleased]`。

### build.rs 生成的东西

修改这些文件后必须重新构建：

- `src/prompts/{gqy.md,gqy.hint.md,gqy-dialogs.md}` → XOR + base64 后编译进二进制（默认人格）。
- `assets/o200k_base.tiktoken` → 紧凑 o200k BPE 词表。
- `assets/jieba/dict.txt` → FST 紧凑词典（记忆分词）。
- `src/`、`web/` 任一文件变化都会生成新的 `GQY_BUILD_ID`，CLI 用它判断 daemon 是否需要重启。

## 3. 测试

```bash
cargo test
```

测试集中在：

| 位置 | 内容 |
|---|---|
| `tests/daemon_reload.rs` | daemon reload 集成测试（隔离 GQY_HOME/XDG_RUNTIME_DIR） |
| `tests/renderer_worker.rs` | 渲染 worker 协议、CJK/表格/Emoji、峰值内存 <100 MiB |
| `testkit/persona-ab/` | 人格提示 A/B 测试工具 |
| `testkit/dev-smoke/` | dev REPL + daemon 冒烟测试 |
| `testkit/toolcall-bridge/` | `tool-call` 工具桥目录同源测试 |
| 各模块内 | 大量单元测试（config、agent、render、tools 等） |

代码审查报告：`docs/code-review-2026-08-16.md`（五轮，含优先级总表）。设计文档：`docs/理念.md`、`docs/cache-and-prompt-plan.md`、`docs/compact-plan.md`。

## 4. 代码地图（源码 src/）

每个模块目录下的 `mod.rs` 只放模块声明和最核心的类型，具体职责分在同级文件
里；**每个文件开头的 `//!` 说明它为什么单独存在**，找东西先读那一段。

| 目录 | 职责 | 里面怎么分 |
|---|---|---|
| `main.rs` / `lib.rs` | 入口：renderer worker 判定、i18n、CLI | `run()` |
| `cli/` | CLI 命令与 REPL 前端 | `args` 参数定义、`localize` 帮助中文化、`daemon_cmds` daemon 子命令、`setup` 首次初始化、`inline_picker` 行内选择器、`usage_view` 用量概览 |
| `cli/repl/` | 交互式 REPL | `remote/`（`one_shot` 单次 / `interactive` 主循环）、`live_turn` 本地事件泵、`layout` 光标与行数、`placeholder` 输入占位符、`commands` 斜杠命令表、`session` 会话切换、`jobs` 后台任务 |
| `daemon.rs` | daemon 纯转发 | → `web::run` |
| `web/` | daemon 本体、HTTP API、SSE | `sessions` / `turns` / `attachments` / `assets` / `persona` / `config_api` / `security` / `qq_history`、`actor` 回合 actor、`event_map` 事件翻译、`ipc_server`、`dto` 脱敏投影、`tty` 回写终端 |
| `ipc/` | UDS JSON 帧 IPC | `protocol` 帧与命令、`launch` 启动配置两阶段提交、`lifecycle` 启停探活 |
| `config/` | 配置结构、加载/校验/迁移 | `defaults` serde 默认值、`provider` / `platform` / `platform_plugins` / `tool_plugins` 结构、`io` 读写迁移、`provider_ops` / `platform_ops` 改名连带、`persona_paths` |
| `config_tui/` | 配置 TUI | `widgets` 框架、`providers` / `platforms` / `personas` / `plugins` / `real_context` / `plugin_settings` / `quota` / `settings` 各编辑界面 |
| `agent/` | 消息组装、工具循环、压缩/redo | `context` 上下文组装（字节纯度核心）、`prompt` 提示词、`history` 消息数组、`turn_loop/` 主循环、`pruning` 溢出三级处置、`control` 并发守卫、`journal` 流水、`reasoning` 推理标题、`artifacts` / `reports` / `images` / `describe` |
| `agent/compact.rs` · `overflow.rs` | LLM 压缩 / 溢出检查 | `Compactor`、`OverflowCheck` |
| `llm/openai_compatible/` | Chat/Responses/Anthropic 客户端 | `builder` 构造、`variants` 思考变体、`chat` / `anthropic` 两条线、`protocol` 协议判定、`endpoints` 端点调度、`errors` 失败分类、`wire` 线上结构、`lower` 消息降级、`sse` 流解析、`dsml` 文本工具调用 |
| `tools/*` | 工具实现 | 注册表在 `tools/mod.rs`；大工具各自成目录（`web/`、`web_images/`、`knowledge_base/`、`memes/`、`archlinux/`、`scripts/`） |
| `tools/descriptions/*.json` | 工具描述真相源 | 81 个工具 + groups.json |
| `state/` | SQLite 会话库、迁移、usage | `conversation_db/`（`types` / `sessions` / `turns` / `queue` / `history` / `attachments` / `platform`）、上层 `StateStore` 同样分领域 |
| `memory/` | 记忆与整理 | `schema` 建表迁移、`search` 分词与语义、`validate` 组织器输出校验、`association` 联想段、`recall` / `write` / `evicted` |
| `platforms/` | 平台无关核心 | `PlatformRuntime`、`run_platform_turn`；`scheduling` 并发限流、`reply` 回复整形、`activity` 热度记账、`logging` |
| `platforms/onebot/` | OneBot/NapCat 适配 | `connection` 反向 WS、`inbound` 解析与限额、`identity` 群名昵称、`admission` 准入、`turn` 建回合、`adapter`、`outbound`、`notices`、`group_join`、`files`、`caches` |
| `platforms/plugins/*` | 平台插件链 | `mod.rs` 的 trait；`real_context/`（`runtime` / `history` / `targeting` / `decision_log` / `affection/`）、`renderer/`（`worker` / `markdown` / `layout` / `paint` / `fonts`）、`message_history/` |
| `render/` | 终端 Markdown/公式渲染 | `stream/` 流式总入口、`markdown` / `table` / `code` / `patch` / `command` / `tool_display` / `usage` / `style` |
| `shell/*` | fish/bash/zsh 集成 | |
| `transfer/*` | export/import | |

### 文件规模基准

量出来的，不是拍的：18 个公认高质量 crate 的 1095 个源文件，中位数 183 /
P90 935 / P95 1412。于是 **目标 800、上限 1500、红线 2000**。

`bash test_scripts/refactor-check.sh` 一次跑完全部检查（全仓 fmt、编译、用例数
不减少、模型面英文、文件规模、依赖方向）。改完代码跑一遍，红了就别提交。

## 5. 修改前的四条铁律（务必读）

1. **前缀即契约**：消息组装路径、工具目录、提示词必须是字节确定的；不要插入动态时间戳到历史中段。
2. **Append-only**：上下文只准尾部追加；压缩是唯一例外且必须单调。
3. **权限由代码承担**：不要在提示词里“授予”权限；真实鉴权在 `platforms/access_control.rs` 和执行层。
4. **三份内容分离**：`raw_content` 只给记忆用；不要往里面塞工程 XML/时间戳。

违反前两条的改动，测试可能不报错，但会让长会话缓存命中悄悄归零、费用暴涨。相关回归测试和 mock 前缀断言见 `docs/cache-and-prompt-plan.md`。

## 6. 提交流程建议

- 小修：直接提交，说明动机与影响。
- 新工具/新配置：同时改描述 JSON、配置默认/校验、TUI（如需要）和测试。
- 涉及消息组装/工具目录/提示词：先跑 `cargo test`，再手测两轮请求的 cache log（`cache-usage.*.jsonl`），确认第二轮的 `cache_read` 没有异常下降。
- 版本发布：按 [18-Nix安装开发与发布](18-Nix安装开发与发布.md) §4 升版本、打 tag，云端构建发布。
- 许可证：MIT，第三方字体/词表/表情等见各自 LICENSE。

## 7. 开发小贴士

- `gqy daemon logs request` 能实时看每个出网请求的 provider/model/scope/token/KB。
- `GQY_LOG=debug` + `--debug` 看完整本地日志。
- `GQY_HOME=/tmp/gqy-test` 隔离测试数据，不会碰你的真实配置。
- 修改 WebUI 静态资源后必须重新编译（文件是 `include_str!`/`include_bytes!` 嵌入的）。
- 测试渲染 worker 时不需要真显示器；`__renderer-worker` 可以独立跑。
