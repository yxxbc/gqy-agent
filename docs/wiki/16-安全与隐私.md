# 16 · 安全与隐私

> ⚠️ **临时版本提示**：本文档由 DeepSeek 根据当前 顾清影 源码逐行整理，属于临时参考版本；正式版文档仍在施工中，内容可能与最新代码存在出入。请以 `gqy -h`、`gqy config`、仓库 README 及源码为准。

顾清影 会读文件、跑命令、访问网络、接入 QQ。它的安全设计原则是：**权限由代码判定，危险操作默认谨慎，数据尽量只留在本机。**

## 1. 你的密钥和隐私数据在哪里

- 配置：`~/.gqy/config/config.jsonc`（目录权限 0700）。
- API key 支持 `$env:VAR` 形式，建议敏感 key 放环境变量而不是明文配置。
- Web 密码存在 `state/web-passwords/`（0600），daemon 启动状态文件**不含明文密码**。
- 数据库、日志、缓存都在 `~/.gqy`（权限 0700）。
- 导出归档是**明文 tar.gz**，默认带密钥；权限 0600。发出去前请用 `--no-secrets`。

## 2. LLM 数据边界

- 你的对话、记忆、文件内容会发送给你配置的模型供应商。顾清影 无法把数据“只留在本地”运行大模型。
- 默认 opencode 公共模型意味着内容经过公共网关；注重隐私请配置自己的 API。
- `cache-usage` 日志默认只记 token 数字，**不记提示词正文**。
- `GQY_LOG_REQUESTS=1` 或 `daemon logs request` 会记录完整请求体，排查完记得关。
- QQ 群聊上下文、消息历史会进入模型上下文，所以不要用 顾清影 的 QQ 群收发秘密信息。

## 3. 命令与文件操作护栏

| 操作 | 护栏 |
|---|---|
| 执行命令 | 1–120 秒超时；独立进程组可整组杀；输出 8 MiB 上限；`tools.command_deny` 黑名单 |
| 删除文件 | 默认走回收站 `trash_path`；拒绝 `/`、家目录、工作区根、Trash 自身等 15 类危险目标；提示词要求先列清单确认 |
| 安装软件 | 提示词要求先确认；AUR 安装必须 PKGBUILD 审查通过且用户确认 |
| 文件读取 | 50 KiB / 2000 行 / 单行 2000 字符；平台文件 128 KiB；二进制拒绝 |
| 搜索根目录 | 排除 /dev /proc /sys /run /tmp 和大量缓存目录 |
| 写文件 | UTF-8 校验；原子写入 |
| Artifact | 目录 0700/文件 0600；拒绝 symlink 与路径逃逸 |
| 搜图下载 | 禁代理、防 SSRF、DNS 固定、图片魔数/尺寸/解码校验 |
| 网页抓取 | 5 MiB 上限 |

## 4. 权限模型

### 终端/WebUI（Owner）

- 本地用户视为 Owner：完整工具、完整记忆权限。
- WebUI 密码保护可选；没有密码时**不要**把 `0.0.0.0` 暴露到不可信网络。公网用 HTTPS 或 `--bind 127.0.0.1`。
- 登录限流：同 IP 60 秒内失败 5 次锁 429。
- 多用户：成员账号凭管理员的一次性邀请码注册（邀请码只存 sha256，8 位、默认 7 天）；密码 PBKDF2-HMAC-SHA256 存库；会话、事件流、用量按账号隔离，`/api/dash/*`、`/api/config`、供应商、语音、QQ 等管理接口成员一律 403。成员对话时的记忆按 principal（`web:<账号 id>`）隔离，与 QQ 群成员同一套规则。成员与管理员跑在同一台机器、同一套工具面上，没有沙盒。

### QQ 平台

- 只信 QQ 号，不信昵称；身份映射会拦截冒名者。
- 管理员：静态 `admin_users` 或动态授权，免限流，能用宿主工具和管理命令。私聊里能读全部记忆，群聊里只读自己的和公开的（群里的回复所有人都看得见）。
- 主人：`owner_users`，必须同时是管理员，只能在配置里写、不能动态授予。私聊里与终端共享记忆和用户资料。
- 群聊非管理员**永远无法**使用发消息/@人等宿主工具。
- 私聊白名单才能开非管理员宿主工具。
- 游客有限流：私聊 2/600s，群聊 2/600s。
- 平台工具集是受限注册表：没有 shell/本地文件/记忆/知识库，避免“QQ 一句话删你电脑文件”。

## 5. 提示词安全

- 默认人格有内容红线：不碰宗教政治时局、黄赌毒、带坏未成年人话题、恋爱模拟等。
- QQ 有 moderation 审核：默认开启，命中严重度 ≥7 强制触发并附审核提示；Base64 藏内容也会被解码检查。
- QQ 历史是“不可信数据”，real_context 和 judge 的提示词明确声明不可信输入、工具返回数据需判断。
- 所有用户昵称/正文做控制字符清理，防终端转义注入。

## 6. 传输与本地 IPC

- OneBot token 用 SHA-256 摘要比较；WS 帧 16 MiB 上限。
- 平台附件用 5 分钟随机 token 租约 URL，最大 50 MiB、最多 128 个。
- IPC socket 在 0700 目录；帧上限 24 MiB；管理命令需要 AdminResult。
- WebUI 永远要登录：首次访问用内置账号（用户名 `gqy`，密码 `gqy`）登录并创建管理员账号，建完号内置账号即失效；之后只认账号（密码 PBKDF2 存库），别人凭一次性邀请码注册。启动参数里没有任何密码。
- 成员沙盒（Landlock + 进程内路径守卫）：成员回合里能写的只有自己家里的 `home/<用户名>/workspace/`、`/tmp` 与脚本缓存目录；能读的除此之外只有跑程序必需的系统目录（`/usr` `/etc` `/proc` `/dev` 这些）、脚本目录和 gqy 自己的二进制——管理员的家、`~/.gqy` 里的配置与数据库都读不到。子进程（`run_command`、后台任务、脚本）靠 Landlock 挡，规则在 exec 前装好、随 exec 继承；daemon 进程内的 `read` `edit` `glob` `grep` `print_image` 看图这类工具靠同一份策略的路径守卫挡（软链和 `..` 绕不出去）。内核没有 Landlock（5.13 以下或被禁）时成员的命令直接被拒，不会裸奔。管理员、终端、QQ 等平台回合默认不套沙盒。只管文件系统，不管网络（成员子进程仍能连 docker.sock、X11、dbus 这类 socket，未做）。用 claude-code / codex / antigravity 这类 CLI 供应商时，沙盒回合里整个 CLI 进程关进同一套 Landlock 规则（它自带的 Bash/Edit 子进程继承），只多放行 CLI 自己的配置目录（`~/.claude`、`~/.claude.json`、`~/.codex`、`~/.gemini`）——所以成员在 CLI 的 Bash 里读得到这些文件（含 CLI 的登录态），这是这条路径的固有代价。
- 会话沙盒 `/sandbox <路径>`（管理员，终端与 WebUI 都能用）：把当前会话关进一个目录，之后的回合里她只能在这个目录里读写——`run_command`、后台任务、脚本、CLI 供应商的整个进程都套同一套 Landlock，`read` `edit` `glob` 这些进程内工具走同一份路径守卫。**读也锁**：家目录其余的东西（`~/.ssh`、`~/.config`、`~/.gqy` 的配置与库）一概摸不到，只放行跑程序必需的系统目录、`/tmp`、顾清影 自己的产出目录（artifact 库、生图落盘）和设置里「沙盒额外可写 / 只读」两份清单（默认可写 `~/.cargo` `~/.npm`，只读 `~/.rustup` `~/.local` `~/.gitconfig`，不然锁进项目后连 `cargo build` 都跑不了；子进程的 HOME 换成沙盒根，`CARGO_HOME` `RUSTUP_HOME` `npm_config_cache` `GIT_CONFIG_GLOBAL` 指回真家，`~/.cargo/bin` `~/.local/bin` 补进 PATH）。刻意不放行的代价：沙盒会话里 `git push` 走 SSH 会失败（读不到 `~/.ssh`），要推就换个没绑沙盒的会话，或者自己把 `~/.ssh` 加进只读清单。没有开关：会话只有「没绑」和「绑在某目录」两态，`/sandbox clear` 解绑，换目录就是重绑；沙盒根与放行摘要写在系统提示词的 `<host-environment>` 里，绑定/解绑各掰一次前缀缓存。绑定时就探测内核，没有 Landlock 当场拒绝；成员会话不能绑（他们本来就固定在自己家里）。重绑等于换工作目录，claude-code / codex 这类按目录归档会话的 CLI 供应商下一轮可能续传失败，走整段重放兜底。
- 登录态：令牌的 sha256 与身份落在 `state/web-sessions.json`（0600），daemon 重启不掉线；服务端与 cookie 都保留 30 天。

## 7. 你可以做的加固

- 配置自己的 API，并定期轮换 key。
- 用 `$env:` 引用 key。
- WebUI 加密码；只本机用就 `--bind 127.0.0.1`。
- QQ token 设长随机值；开启白名单；非必要不开非白名单游客。
- 备份用 `gqy export --no-secrets` 再传。
- 定期看 `gqy daemon logs request`（临时）或用量明细，确认没有异常请求。
