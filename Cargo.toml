[package]
name = "gqy"
version = "0.6.0"
edition = "2021"
rust-version = "1.89"
license = "MIT"
repository = "https://github.com/yxxbc/gqy-agent"
description = "Command-line AI assistant"
autobins = false

[lib]
name = "gqy"
path = "src/lib.rs"

[[bin]]
name = "gqy"
path = "src/main.rs"

# 语音前端进程:麦克风/唤醒词/本地识别/提示音全部住在这里,主程序 `gqy`
# 不链接 sherpa-onnx(静态库 ≈30MB)。只有 `--features voice` 才会构建。
[[bin]]
name = "gqy-voice"
path = "src/bin/voice.rs"
required-features = ["voice"]

[dependencies]
anyhow = "1.0"
axum = { version = "0.8", features = ["ws"] }
base64 = "0.22"
blake3 = "1.5"
chrono = { version = "0.4", features = ["serde"] }
clap = { version = "4.5", features = ["derive"] }
cosmic-text = { version = "0.19.0", default-features = false, features = ["std", "swash"] }
crossterm = "0.28"
directories = "6.0"
hex = "0.4"
html2text = "0.16.7"
htmd = "0.5.5"
image = { version = "0.25", default-features = false, features = ["bmp", "gif", "jpeg", "png", "webp"] }
json_comments = "0.2"
markup5ever_rcdom = "0.38"
# 快递 100 的请求签名是 MD5(param + key + customer)——只当摘要用,不是安全原语。
md-5 = "0.10"
futures-util = "0.3"
flate2 = "1.0"
fuzzy-matcher = "0.3"
fancy-regex = "0.17"
fst = "0.4"
fontdb = { version = "0.23", default-features = false, features = ["std", "fs", "memmap"] }
if-addrs = "0.13"
libc = "0.2"
pulldown-cmark = { version = "0.13.4", default-features = false }
# Local embedding inference. ONNX Runtime is loaded at runtime from the system
# library (`onnxruntime-cpu` on Arch); nothing is downloaded at build time, so
# `cargo build --locked` stays offline-clean. The tokenizer uses the pure-Rust
# regex engine to avoid a C build dependency.
ort = { version = "2.0.0-rc.13", default-features = false, features = ["load-dynamic", "std"] }
tokenizers = { version = "0.23", default-features = false, features = ["fancy-regex"] }
reqwest = { version = "0.12", default-features = false, features = ["blocking", "json", "multipart", "rustls-tls", "stream", "gzip", "deflate"] }
ratatui = { version = "0.29", default-features = false, features = ["crossterm", "unstable-rendered-line-info"] }
rand = "0.8"
rpassword = "7.4"
rodio = { version = "0.19", default-features = false, features = ["mp3", "wav"] }
rusqlite = { version = "0.32", features = ["bundled"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"
sha1 = "0.10"
sha2 = "0.10"
termimad = "0.34"
tempfile = "3.15"
tar = "0.4"
tokio = { version = "1.38", features = ["io-std", "io-util", "macros", "net", "process", "rt-multi-thread", "signal", "sync", "time"] }
tokio-util = { version = "0.7", features = ["io"] }
trash = "5.2"
tracing = { version = "0.1", default-features = false, features = ["std"] }
tracing-appender = "0.2"
tracing-subscriber = { version = "0.3", default-features = false, features = ["fmt", "registry"] }
unicode-segmentation = "1.12"
unicode-width = "0.2"
urlencoding = "2.1"
vte = "0.15"
rustc-hash = "2"
yaml-rust2 = { version = "0.11", default-features = false }
ratex-parser = "0.1.12"
ratex-layout = "0.1.12"
ratex-types = "0.1.12"
ratex-render = { version = "0.1.12", features = ["embed-fonts"] }
# 语音(voice feature,默认关):本地唤醒词 + VAD + SenseVoice 识别。
# 只有 gqy-voice 二进制会启用;主程序永远不带。
sherpa-onnx = { version = "1.13.7", optional = true }
cpal = { version = "0.15", optional = true }
pinyin = { version = "0.10", optional = true }
bzip2 = { version = "0.4", optional = true }

[features]
default = []
voice = ["dep:sherpa-onnx", "dep:cpal", "dep:pinyin", "dep:bzip2"]

[build-dependencies]
base64 = "0.22"
fst = "0.4"

[dev-dependencies]
jieba-rs = "0.7"
tiktoken-rs = "0.12"

[profile.release]
lto = "thin"
strip = "symbols"
# 单编译单元：跨单元内联与去重充分化，.text 实测显著缩小（见
# docs/plan-is-true/low-footprint.md 第 3 轮）。代价只是 release 编译更慢。
codegen-units = 1
