{
  "name": "run_command",
  "display_name": "运行命令",
  "description": "Run a shell command or a complex shell script. Use background=true with a short title for long work: it returns a job_id immediately; check progress with job(action=status), stop with job(action=stop), and you are notified automatically when it finishes. Inside scripts, `gqy tool-call <name> --stdin` calls structured tools as this session. Internal calls execute in the daemon-side session workspace: they do not inherit the script's environment or working directory, so pass data between tools via argument JSON or files. To delete files, use trash_path instead of rm so they can be restored.",
  "parameters": {
    "type": "object",
    "properties": {
      "command": {
        "type": "string",
        "description": "The shell command or script to run. May contain complex multi-line commands; prefer edit for file edits."
      },
      "timeout_seconds": {
        "type": "integer",
        "description": "Optional timeout in seconds. Ignored when background=true."
      },
      "background": {
        "type": "boolean",
        "description": "Run detached in the background and return a short job_id immediately. Suitable for commands expected to take longer than about 30 seconds."
      },
      "title": {
        "type": "string",
        "description": "Short title for the background command (at most 16 characters), shown in the UI status line, e.g. “release build”. Recommended when background=true."
      }
    },
    "required": [
      "command"
    ]
  },
  "permission": "readonly",
  "always_loaded": true,
  "load_policy": "summary",
  "groups": [
    "shell"
  ],
  "timeout_seconds": 0
}
