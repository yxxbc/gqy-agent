{
  "name": "remember_fact",
  "display_name": "记录记忆",
  "description": "Remember a fact or useful knowledge long-term in GQY's memory. Use kind=correction when the user corrects something you said or did.",
  "parameters": {
    "type": "object",
    "properties": {
      "content": {
        "type": "string",
        "description": "Concise fact or knowledge point to remember."
      },
      "source": {
        "type": "string",
        "description": "Optional source label."
      },
      "kind": {
        "type": "string",
        "enum": [
          "fact",
          "correction"
        ],
        "description": "fact (default) or correction."
      },
      "reason": {
        "type": "string",
        "description": "Required for kind=correction: why you got it wrong."
      }
    },
    "required": [
      "content"
    ]
  },
  "permission": "writes",
  "always_loaded": true,
  "load_policy": "summary",
  "groups": [
    "memory"
  ]
}
